name: Dashboard Casa Ayman
version: "1.1.4"
slug: dashboard_casa_ayman
description: Dashboard personalizzata per Casa di Ayman & Martina
url: https://github.com/AymanNass/ha-dashboard-ayman
arch:
  - aarch64
  - amd64
  - armv7
init: false
ingress: true
ingress_port: 3000
panel_icon: mdi:view-dashboard-variant
panel_title: Dashboard
ports:
  3000/tcp: 3000
ports_description:
  3000/tcp: Web UI port — optional. Leave empty to use Ingress only; set a host port (e.g. 3000) for direct/kiosk access.
options: {}
schema: {}
